import objetos.*
import wollok.*

describe "futbolito" {


    test "buscarla" {
        assert.notEquals(pelota.position(),lionel.position())
        lionel.buscarla()
        assert.equals(pelota.position(), lionel.position() )


    }
}